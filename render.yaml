# Render.com Blueprint — ToolHub
services:
  - type: web
    name: toolhub
    runtime: node
    plan: free
    buildCommand: node build.js && npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20

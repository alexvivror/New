# Render.com Blueprint — USATools
# Deploy: connect repo to Render, it auto-detects this file
services:
  - type: web
    name: usatools
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 20
